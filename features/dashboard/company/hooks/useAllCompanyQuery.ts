"use client"

import { useState } from "react"

export const useAllCompanyQuery = () => {
  const [isLoading, setIsLoadjng] = useState(false)
  const [error, setIsError] = useState("")
  const [companies, setCompanies] = useState
}
